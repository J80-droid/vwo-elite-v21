// Chemistry Lab Module Imports - Centralized Registration
// Registration is now handled in api/registry.ts to avoid circular dependencies
// This function is kept as a no-op for backward compatibility if called elsewhere.

export const registerChemistryModules = () => {
  // No-op: Modules are registered in api/registry.ts
};
