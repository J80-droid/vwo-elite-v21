// Modules are imported in registry.ts currently, but this file can serve
// as an aggregate export if needed in the future.
// Currently used as a side-effect import in Layout to ensure registration if we switched patterns,
// but our current pattern registers in registry.ts.
// Keeping it to match the pattern of other labs.
